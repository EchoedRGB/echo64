#include "screen.cpp" // Include your new file

int main() {
    startScreen();

    // Example coordinates
    //addCoordinate(4, 5); // Add a coordinate as an example
    //addCoordinate(2, 3); // You can add more coordinates for testing
    //addCoordinate(63, 63);

    run(); // This will handle the main event loop

    return 0;
}
